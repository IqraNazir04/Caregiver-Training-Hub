# Caregiver Training Hub

Bite-sized microlearning + AI chat support for home health aides and family caregivers of elderly or chronically ill relatives. Most caregiving falls to people with no formal training — this is a place to build that knowledge in small pieces, ask questions grounded in real guidance, and track progress toward a certificate, without wading through a hundred-page pamphlet.

## What's built

- **22 condition/skill-specific learning tracks** across 5 themes — short 3-5 minute lessons plus quizzes:
  - **Foundational / Getting Started** — Dementia & Alzheimer's Care, Diabetes Management, Post-Stroke Recovery, Caregiver Roles & Boundaries, Building a Daily Care Routine
  - **Practical Skills** — Medication Management & Tracking, Warning Signs to Watch For, Basic First Aid for Caregivers, Mobility Assistance & Fall Prevention, Nutrition & Meal Planning
  - **Communication & Behavior** — De-escalating Agitation, Communicating Through Cognitive Decline, Talking to Doctors, Setting Boundaries with Family
  - **Emotional & Mental Load** — Recognizing Burnout, Managing Guilt & Grief, Building a Support Network, Realistic Self-Care
  - **Family & Logistics** — Coordinating Family Care, Legal & Financial Basics, Planning for Respite Care, End-of-Life Planning
- **Track-scoped and general RAG chat**: caregivers ask questions and get answers grounded in seeded reference content, generated by **Claude Sonnet 5**, with inline citations, a visible "not medical advice" disclaimer on every response, and lightweight keyword-based escalation logging for emergency-adjacent messages. Chat answers render real markdown (headings, bold, bullet lists), not raw `**`/`-` characters.
- **Progress tracking**: quiz attempts are recorded per lesson (retakes upsert the latest score) and rolled up into per-track completion counts shown as progress bars.
- **Certification**: once every lesson in a track is completed, a real printable certificate is available (`/certification`, `/certification/:slug`) with the caregiver's name, the course, and the actual completion date.
- **Public course browsing**: a `/courses` page lists every track for unauthenticated visitors (via `GET /tracks/public`), linking to sign-up.
- **Profile-based topic picker**: caregivers pick "what are you caring for" on their profile; the Tracks page surfaces a "Recommended for you" section based on that, plus keyword search across all tracks.
- **Auth**: email/password signup and login with JWT sessions.
- **Public marketing pages**: Home, About, Features, Courses, FAQ — plus an authenticated app shell (Tracks, Certification, Chat, Profile).

## Roadmap (not yet built)

Daily care checklist generator, medication OCR/interaction assistant, full 3-tier symptom triage, voice-first mode, burnout/respite check-ins, family care coordination board, pre-appointment prep tool. (See the Features page for status of each.)

## Tech stack

- **Backend**: FastAPI + SQLAlchemy (SQLite) + Pydantic, JWT auth (`python-jose`, `passlib`/bcrypt)
- **RAG**: Chroma (vector store) + OpenAI `text-embedding-3-small` for embeddings, **Claude Sonnet 5** (Anthropic Messages API) for chat generation — split across two providers since Anthropic has no embeddings API
- **Frontend**: React + Vite, React Router, Context-based auth state, plain CSS (no component library), hand-drawn inline SVG illustrations (no external image assets)
- **Tests**: pytest + FastAPI `TestClient`, with all OpenAI/Anthropic calls mocked

## Project layout

```
backend/
  app/
    routers/        auth, tracks, chat
    rag/             ai_clients (OpenAI embeddings + Claude chat), retrieval, prompts, ingest, safety
    models.py, schemas.py, security.py, deps.py, config.py, database.py
  seed/
    content/         one file per track (22 total)
    seed_data.py      idempotent loader — SQL rows + Chroma embeddings
  tests/

frontend/
  src/
    pages/           Landing, About, Features, Courses, Faq, Login, Signup,
                      TrackList, TrackDetail, Chat, Profile, Certification, CertificateView
    components/       CaregivingScene, AboutScene, SideDecor (hand-drawn SVG), icons
    api/client.js     fetch wrapper
    auth/AuthContext.jsx
```

## API overview

| Area | Routes |
|---|---|
| Auth | `POST /auth/signup`, `POST /auth/login`, `GET /auth/me`, `PATCH /auth/me` (update selected tracks) |
| Tracks | `GET /tracks` (authenticated, with progress), `GET /tracks/public` (unauthenticated preview), `GET /tracks/{slug}`, `GET /tracks/{slug}/lessons/{lesson_id}`, `POST /tracks/{slug}/lessons/{lesson_id}/quiz/submit` |
| Chat | `POST /chat/sessions`, `GET /chat/sessions/{id}/messages`, `POST /chat/sessions/{id}/messages` |

Full interactive docs at `/docs` once the backend is running.

## Data model

`User` (with `selected_tracks`), `Track` (themed), `Lesson`, `QuizQuestion`, `SourceDocument` (chunked/embedded into Chroma), `LessonProgress` (per-user quiz score + `completed_at`, used for both progress bars and certificate eligibility/date), `ChatSession`, `ChatMessage` (citations + flagged state), `EscalationLog`.

## Running locally

### Backend

```bash
cd backend
python3 -m venv .venv && source .venv/bin/activate
pip install -e .
cp .env.example .env   # then set OPENAI_API_KEY (embeddings) and ANTHROPIC_API_KEY (chat, Claude Sonnet 5)
python -m seed.seed_data   # loads all 22 tracks/lessons/quizzes into SQLite and embeds source docs into Chroma
uvicorn app.main:app --reload
```

API docs at http://localhost:8000/docs.

Required env vars (`backend/.env`): `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `JWT_SECRET`, `JWT_EXPIRE_MINUTES` (default 1440), `DATABASE_URL` (default `sqlite:///./caregiver.db`), `CHROMA_PERSIST_DIR` (default `./chroma_data`).

### Frontend

```bash
cd frontend
npm install
npm run dev
```

App at http://localhost:5173 (proxies API calls to http://localhost:8000 by default — override with `VITE_API_URL`).

### Tests

```bash
cd backend
pytest
```

Backend tests mock all OpenAI and Anthropic calls, so no API key or network access is required to run them.

## Safety notes

Every chat response carries a visible disclaimer that it is educational, not medical advice, and to call 911 for emergencies. Messages that match a small set of emergency-adjacent keywords are flagged in the UI and logged to an `EscalationLog` table for later review — this is a lightweight placeholder for the planned full 3-tier symptom triage feature, not a clinical triage system.

All lesson, quiz, and source-document content is sample content written in the style of trusted sources (NIA/CDC/Mayo Clinic-style guidance) for demonstration purposes — it is not scraped or verbatim from those organizations, and is not a substitute for professional medical advice.
